<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.cinema.mapper.TicketMapper">
 
 <select id = "get" resultType="com.cinema.domain.TicketVO">
 
 select * from ticket where ticketNO=#{ticketNO}
 
 </select>
 
 <select id = "getAllTicket" resultType="com.cinema.domain.TicketVO">
 
 select * from ticket where memberNO=#{memberNO}
 
 </select>
 
 <insert id = "ticketing">
 
  insert into movie_ticket (ticketNO,memberNO,id, reserveSequence, title, runningTime, movieDate, ticketNumber, selectedSeat, selectedTheater)
  values (seq_ticket.nextval,#{memberNO},#{id}, #{reserveSequence}, #{title}, #{runningTime}, #{movieDate}, #{ticketNumber}, #{selectedSeat}, #{selectedTheater})
 
 </insert>
 
 <delete id = "cancel">
 	
 	delete from ticket where ticketNO=#{ticketNO}
 
 </delete>

 </mapper>